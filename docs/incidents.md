# Incident log (escape-rate ledger)

One line per incident: what fired, WHO NOTICED FIRST (machinery | human |
agent-session), resolution, detector added. Reviewed at month-ends. Seeded
retroactively 2026-08-21 from the full project history.

**What counts as an ESCAPE (purpose-scoped rule, ratified by the user
2026-09-02).** An escape is a defect caught by a **human, or a later/different
agent-session** — i.e. by something OTHER than the responsible machinery —
*before* that machinery caught it. The metric measures how often failures evade
the machinery and reach a stable, consequential state before being noticed. A
drift **introduced AND self-caught within the same authoring session**, before
any independent party or scheduled check encountered it, is a **CORRECTION**
(fixed in place, not a logged escape) — counting normal edit-cycle self-review as
escapes would drown the metric in churn and stop it measuring machinery-vs-
reality. The guard against an agent hiding self-caught drift is **a detector per
class** (the closed defect loop), NOT tally-fiddling: an un-self-caught drift is
still captured — by the scheduled machinery (a WIN) or by a human (an ESCAPE) —
so nothing that reaches a stable state escapes accounting. (The prior wording —
"agent-session always counts as an escape" — is superseded; the existing rows are
unaffected, as each already persisted past its authoring session.)

| Date | Incident | Noticed first by | Resolution | Detector added |
|---|---|---|---|---|
| 2026-07-31 | Tick ran without writing launchd log (route unknown) | agent-session (ESCAPE) | benign; did not recur | none (class retired with launchd) |
| 2026-08-04 | DNS outage at tick; no fetch; primary receipt for 08-05 lost | machinery (heartbeat FAIL) + agent-session same day | recovery tick; honest miss | fetch retries +5m/+15m + tests |
| 2026-08-05..06 | Machine moved Madrid→Tampa; local-time schedule silently wrong; 08-07 receipt lost | agent-session (ESCAPE) | server migration; Madrid-keyed market_today() | market_today test; server is sole writer |
| 2026-08-10 | GitHub silently stopped accepting the Mac's SSH key; morning push failed | agent-session (ESCAPE) | Mac remote moved to HTTPS/gh | none — class mitigated by demoting Mac; gh-token expiry queued in continuity note |
| 2026-08-19..20 | Server SSH lockout by co-tenant hardening; ALERT_* secrets undeliverable; ~1 week without inbox digests | human (ESCAPE) | new esios-vps identity; least-privilege access | INFRA.md contract queued; dedicated-server triggers defined |
| 2026-08-20 | Hetzner egress block on SMTP 465; first server email send timed out | agent-session at live test (ESCAPE) | switch to 587+STARTTLS | port checked in code path; tests updated |
| 2026-08-20 | Artifact publish version conflict | machinery (409) | resynced baseline | n/a (artifact deprecated for Pages) |
| 2026-08-21 | OTS manifests silently rewritten by same-day re-ticks; 12/13 proofs invalidated (live defect in ots_stamp) | machinery (independent auditor) | stamped originals restored from git history; stamp logic made immutable-per-slot | regression test test_ots_stamped_manifest_is_immutable* |
| 2026-08-23 | ERCOT go-live: ercot.com 403s the Helsinki server (US-geo/datacenter block, whole domain); fetch-retry backoff also stalled the tick ~20min | machinery (live server seed run) | ERCOT de-scheduled from server_tick; DE unaffected; source alternatives under review | de-scheduled; per-market source reachability now checked at wire-time |
| 2026-08-28 | FR (private, derived-only ENTSO-E) day-ahead prices reached the PUBLIC ES mirror: publish_mirror.sh's HARDCODED exclude list (de/ercot/it/pt) was not updated when FR was added, so Data/fr/prices.json + ots were committed and pushed to github.com/ArturoQuintana/spain-dayahead-ledger from FR go-live (~10:30 UTC) until caught ~16:10 UTC. No harm to the record; a redistribution-policy violation for low-sensitivity, widely-available data. Root cause = the deferred Phase-3 drift (publish_mirror not registry-derived) + my omission when building FR. | agent-session (ESCAPE), noticed while republishing the mirror during the ES data migration | publish_mirror ES excludes made REGISTRY-DERIVED (`for s in $(… markets)`, exclude unless es) + `--delete-excluded` to purge; FR removed from the mirror and pushed | test_mirror_is_deny_by_default_allowlist (fails if any per-market exclude is hardcoded) |
| 2026-08-29 | The one-project consolidation migrated DE's history into the PUBLIC `talea` repo as a single bulk commit (fd4bdd49, 2026-08-29), so talea's DE git-history begins with a multi-day sync — the timing "weak check" (committed_at vs the commit that introduced a receipt) no longer corroborates pre-08-29 DE dates from talea alone. VERIFY.md still claimed DE "git-attested (every tick commits and pushes)" and disclosed no DE evidence boundary, though ES's equivalent pre-08-10 bulk-import boundary IS disclosed. (Also: ARCHITECTURE's Automation section still described per-mirror verify workflows.) No arithmetic/money error; per-tick DE evidence still exists in the private repo + the frozen germany-dayahead-ledger mirror. The agent's own "are all docs updated?" sweep this session MISSED both. | machinery (independent auditor — first talea run) | VERIFY.md gains a DE evidence-boundary disclosure parallel to ES's (per-tick history in the private repo / frozen germany-dayahead-ledger mirror; use OTS 08-27+); ARCHITECTURE Automation corrected to one talea repo / one verify.yml --all | test_verify_discloses_migrated_market_evidence_boundaries |
| 2026-08-29 | A second, same-day independent audit pass found two gaps the first pass missed: (1) VERIFY.md's ERCOT tier bullet claimed plain "git-attested from their first tick," but ERCOT's receipts entered the public `talea` repo in the same fd4bdd49 one-project-consolidation commit as DE — the same evidence-boundary gap already disclosed for DE (2026-08-29, above) was left undisclosed for ERCOT. (2) `scripts/render_dashboard.py`'s non-gate tile subtitle read "silent shadow ledger" for every market without the ES-only GBM-v2 gate — i.e. DE and ERCOT's own public dashboard pages, both PUBLIC markets, self-described as "silent," a leftover copy from the original 2026-08-11 silent-shadow-launch plan (docs/shadow-ledgers-de-ercot-scope.md) that was never updated when those markets went fully public. No arithmetic/money error either way. | machinery (independent auditor, second same-day run) | VERIFY.md gained ERCOT's own evidence-boundary disclosure (parallel to DE's, naming commit fd4bdd49); render_dashboard.py's tile subtitle changed from "silent shadow ledger" to "public ledger" | test_verify_discloses_migrated_market_evidence_boundaries (extended) + test_build_de_is_market_aware_without_gate (extended: asserts "silent" never appears) |
| 2026-08-29 | `tests/test_fetcher_contracts.py::test_es_apidatos_hourly_means_from_quarter_hour_values` hardcoded the 2026-08-20T00 hourly mean as 194.59. This resolver's local `uv run pytest` (Python 3.11.15) passed at 194.6 and the resolver committed that value instead — WRONG: the true mean, (201.42+198.52+190.49+187.95)/4, is exactly 194.595, a genuine two-decimal rounding boundary. Which side `round()` lands on depends on the LSB of the intermediate summed float, which itself depends on the interpreter's float-`sum()` algorithm — CPython 3.12+ sums with Neumaier compensation (bpo-100425) and gets exactly 778.38 -> 194.59; 3.11 and earlier sum naively and get 778.3800000000001 -> 194.6. CI runs 3.12.3 and correctly failed the resolver's own first push (`assert 194.59 == 194.6`). Neither value was "the bug" — the test was asserting an interpreter-dependent boundary as if it were a fixed fact. No arithmetic/money error in production `fetch_hourly` either way; `Data/es/prices.json` is fetched once at ingestion by one pinned server interpreter and stays fixed thereafter, so this is a test-fixture portability issue, not a live-ledger reproducibility risk. | machinery (resolver's own pytest gate found it; CI then caught the resolver's own wrong first fix) | Test now asserts `out["2026-08-20T00"] in (194.59, 194.6)` — the interpreter-independent invariant — instead of pinning one side of a boundary the fixture happens to sit on; derivation left inline as a comment | the corrected, version-tolerant assertion itself is the regression guard |
| 2026-08-29 | GB's fetcher returned `HTTP 400` on EVERY scheduled server tick since launch, so GB launched PUBLIC but silently never fetched — empty ledger, "awaiting first settled day" forever. Root cause: a market with no seed `prices.json` fetches from `loop.py`'s default start (~7 months back) in ONE request, but Elexon's market-index endpoint caps the from→to range at 7 days inclusive ("must not exceed 7 days"). GB only ever "worked" in a hand-run 2-day smoke test (one chunk). NO auditor caught it: the ledger auditor treats GB as "launching, exempt"; the infra auditor's outside-in vantage cannot read the server logs where `[…:gb] FETCH-ERROR` appears. Surfaced only when the agent ran down the infra-audit's OPERATOR-VERIFY list on the server (`journalctl -u esios-tick`). Argues for granting the infra auditor a read-only SSH key so this class is caught by machinery next time. | agent-session (ESCAPE — server-log follow-up prompted by the infra-continuity audit's operator-verify list) | `gb.fetch_hourly` now splits any window into ≤7-day chunks and merges; validated live (5,174h fetched over the real Jan→Aug window; GB prices.json seeded on the server). loop.py core untouched. | test_gb_chunks_windows_to_elexon_7day_cap (a 30-day fetch must issue multiple requests, none exceeding the 7-day cap). CLASS-level detector added 2026-08-29: scripts/check_liveness.py now flags any market whose PRICES have gone stale (>2d) as STALE — a broken fetcher no longer hides as the exempt NEVER; it pages every tick (was invisible because a launched market with no data has nothing to verify and no receipt to be stale). Test: test_stale_prices_flag_a_broken_fetch_even_when_never_committed. |
| 2026-08-29 | First infra-continuity audit (validated end-to-end) surfaced: (1) ERCOT/JP GitHub-Actions scheduled runs fired HOURS late (~9.5h / ~6.6h — a known GitHub platform behaviour; integrity is safe — the leak guard makes a late run an honest miss, never a corrupt receipt); (2) JP "never ran" at audit time (self-resolved: it fired 07:11 UTC — first-run cron latency); (3) the 13-date ES OTS final-state gap (2026-08-09..08-21) re-flagged as aging, though its don't-mint decision was already made 2026-08-24. | machinery (infra-continuity auditor, first run) | ercot/jp crons moved EARLIER + off-peak minutes for margin (ercot 09:23 UTC ≈04:23 CT, ~9h pre-publication; jp 21:37 UTC ≈06:37 JST, ~3.5h pre-publication); the 13-date OTS gap RATIFIED 2026-08-29 as an accepted PERMANENT disclosure (do NOT mint — VERIFY.md STATUS line); infra-auditor prompt refined to treat the accepted gap + late-but-successful Actions runs as non-findings and to verify tick freshness by content-timestamp not git-log. | ercot/jp cron comments document the mitigation; VERIFY.md STATUS marker + the auditor's OTS exception prevent monthly re-flagging |
| 2026-08-29 | Public-facing OTS OVERCLAIM: the dashboard renderer (`render_dashboard.py`) stated flatly "OpenTimestamps-anchored" in both templates' header + footer, and VERIFY.md's per-market tier list said DE was "OTS-anchored (Bitcoin) from 2026-08-27". But an OTS proof is "pending" (calendar-only, signed commitment to a FUTURE block) until a Bitcoin block confirms it and the weekly `ots upgrade` pulls the merkle path in. Ground-truth check (`ots info`): only ES's proofs THROUGH 2026-08-22 are Bitcoin-confirmed; ES 08-23→today, and ALL of DE / ERCOT / GB / JP, are still pending — so the newer public markets' pages (and DE's VERIFY.md bullet) claimed a Bitcoin anchor that does not yet exist. The core commit-before-truth claim is UNAFFECTED (git commit + pending proof already establish it; the block only hardens against a later history-rewrite), so this overstates the timestamp's *finality*, not the record's integrity. ERCOT's and GB's own VERIFY.md bullets already disclosed "pending" correctly — the drift was the generated pages + DE's bullet. Surfaced by the user asking whether the public pages overstate the anchoring. | human (ESCAPE — user question, not machinery) | render_dashboard.py both templates changed "OpenTimestamps-anchored" → "OpenTimestamps-stamped — Bitcoin-confirmed within days" with an explicit pending→confirmed footer disclosure pointing to VERIFY.md; VERIFY.md DE bullet rephrased to ERCOT's honest "OTS-stamped … proofs still pending" template. JP needs no public fix (private, pending JEPX licence — not on the mirror). | test_pages_never_overclaim_bitcoin_finality (the generated public pages must never contain "OpenTimestamps-anchored", must say "OpenTimestamps-stamped" + "within days", across BOTH the settled and awaiting templates). VERIFY.md's hand-maintained prose stays guarded by the weekly auditor + on-publish statistical referee — deliberately NOT a static unit test, since DE's claim legitimately BECOMES "Bitcoin-anchored" once its proofs upgrade in days, and a static assertion would then break on a true statement. FOLLOW-UP (2026-08-29): the auditor's OWN prompt embedded the same overclaim (it framed DE/ERCOT as "OTS-anchored from <date>" and its OTS check only verified proof-existence + hash-match, never pending-vs-confirmed) — so machinery structurally COULD NOT catch this class. Both the recurring weekly auditor (trig_01B9d7kGJ3rbKYr4hRPo2vBf) and the GB-first-settlement one-shot (trig_01MkDjywRimMk5JBrpUsrfuH) were strengthened: baseline corrected to "OTS-stamped … may be PENDING", and a new detector added — record each proof's CURRENT ots-info state and FLAG any page/doc claiming "Bitcoin-anchored" for a market whose proofs are ALL still pending. Self-adjusting (a market flips to a true anchored claim once upgraded). Mirror docs/routines/auditor.md byte-synced to the live prompt. This converts the class from human-caught to machinery-caught. |
| 2026-08-24 | The 2026-08-21 restoration was correct-but-incomplete: for 13 dates (2026-08-09 through 2026-08-21) the OTS-stamped `<date>.txt` anchors the FIRST daily tick's state, not that day's final settlement — the `<date>-2.txt` slot created during restoration holds the true final state but was never itself submitted to OpenTimestamps, so those 13 dates currently have no Bitcoin anchor of their final content. VERIFY.md's "every proof now matches" was an overclaim. No data is wrong (arithmetic re-verifies exactly); only the proof coverage is incomplete pre-2026-08-22, when ots_stamp began covering every slot on every tick. | machinery (independent auditor) | VERIFY.md corrected to state the actual coverage; whether to mint new proofs anchoring the current final state for those 13 dates is an evidentiary-claims decision left to the user | scripts/audit_ots_manifests.py + regression test (flags any written manifest slot with no matching .ots) |
| 2026-08-30 | The new constitutional-conformance auditor's first (shakedown) run returned ALL SUBSTANTIVE CLAUSES CONFORMANT — zero VIOLATIONS (leak guard enforced + tested; append-only holds project-wide by full-history replay; registered panel == loop.STRATEGIES with no R1 promotion ever fired; costs net; no unauthorized v2; the p-hacking git-check found no post-data loosening of any frozen criterion; no real stakes; tests strict 259/259) — but DRIFT(3) in governing-doc coverage: (D-1) INFRA.md's cloud-routines map omitted the red-team + constitutional routines and its "four cycle prompts" line was stale; (D-2) the month-end reviewer had no docs/routines/ mirror; (D-3, methodology) none of the auditor/constitutional/red-team prompts instructed checking for a SHALLOW clone before git-history checks — the CCR checkout arrives shallow (~50 commits), which would silently truncate the history the append-only + p-hacking checks need. The auditor itself CAUGHT the shallow clone (implausibly-short history on a twice-daily-ticking file), ran read-only `git fetch --unshallow`, and completed correctly. | machinery (constitutional auditor, first run — a WIN, not an escape) | D-1: INFRA.md map corrected to all SEVEN standing routines. D-2: already fixed hours earlier (see the month-end mirror entry below). D-3: added a SETUP shallow-clone guard (`git rev-parse --is-shallow-repository` → `git fetch --unshallow`) to the ledger auditor, constitutional auditor, and red-team — LIVE routines + docs/routines/ mirrors, byte-synced. | The SETUP guard now sits in the three history-dependent prompts; the constitutional auditor's section D is the standing detector for the routine/doc-drift class (D-1/D-2). |
| 2026-08-30 | The month-end governance reviewer (`trig_01FfwRRAfpNtQQvSdQZnU2Xr`) had silently drifted post-rename: it still fetched from the DEAD `spain-dayahead-ledger` repo at the pre-consolidation single-market path `Data/ledger.jsonl`, so its next run (2026-09-01) would have produced an empty / 404 governance report — and this is the routine that computes the FALSIFICATION triggers, the project's honesty backstop. The esios→talea rename + one-project consolidation updated the code, docs, and the other routines but MISSED this one (it had no `docs/routines/` mirror either, so the rename sweep had nothing to grep). Found by agent-session while reconciling the month-end reviewer's scope against the new constitutional auditor — not by machinery. No output had fired yet (caught ~2 days before its next run). | agent-session (ESCAPE) | routine rewritten: private `talea-ledger` checkout, market-agnostic (scans `Data/*/ledger.jsonl` → multi-market), talea naming; verified it now discovers 5 live ledgers (es/de/ercot/it/pt). | `docs/routines/month-end-review.md` mirror added (it was the ONLY standing routine with no mirror — the gap that let it drift unseen). CLASS detector: the new quarterly constitutional auditor's section D checks every `docs/routines/` mirror + routine points at a LIVE repo/path — no dead/renamed repo, no pre-consolidation path — exactly this class. |
| 2026-08-31 | Independent audit found VERIFY.md and docs/ARCHITECTURE.md stale about GB's evidence tier: GB now has committed receipts (per-tick, from 2026-08-30), a `Data/gb/prices.json`, and OTS manifests from 2026-08-29 (earliest already Bitcoin-confirmed) — but VERIFY.md's GB bullet still said "nothing but its LICENSE.md," and ARCHITECTURE.md's market table still listed GB's public tier as "Pages (git-attested)" with no OTS column entry. Documentation UNDERSTATED GB's evidence (no page overclaimed); no arithmetic/leak/append-only issue. | machinery (independent auditor) | VERIFY.md's GB bullet rewritten to state its actual receipts/OTS/Bitcoin-confirmation status and note GB entered the repo via per-tick commits (no bulk-import evidence boundary, unlike DE/ERCOT); ARCHITECTURE.md's GB row changed "Pages (git-attested)" → "Pages + OTS" | prose-only fix (parallel to the 2026-08-29 DE/ERCOT evidence-boundary entries above); no new automated check — the auditor itself is the recurring detector for this class |
| 2026-08-31 | Independent audit found ERCOT's dashboard understated its real commit gap: `Data/ercot/receipts.jsonl` has no receipt at all for targets 2026-08-29/08-30 (a genuine 2-day tick outage, honest per GOVERNANCE.md), but ercot.html's "Record" tile read "0 missed" because `render_dashboard.py`'s missed-day window was bounded by `[first receipt target, LAST SETTLED target]` — with only one day settled so far, the window never reached the gap, so it stayed invisible for a full settlement cycle. Underlying `receipts.jsonl` was honest and complete throughout; only the dashboard's derived "0 missed" figure was wrong. | machinery (independent auditor) | `render_dashboard.py`'s missed-day window changed to bound by the last RECEIPT target instead of the last settled target, so a gap surfaces the same day it happens | test_build_missed_window_extends_to_last_receipt_not_last_settled (reproduces the exact ERCOT shape: one settled day, receipts either side of a 2-day gap; fails against the pre-fix window bound, passes after) |

| 2026-09-01 | Second infra-continuity audit found: (1) ERCOT's earlier single-cron fix (2026-08-29) still wasn't enough — GitHub Actions fired the 09:23 UTC schedule 6-14h late on 2 of the last 4 scheduled runs, missing the commit window both times (the clock/leak guard correctly refused rather than leak, but 2 of ERCOT's last 4 target days went uncovered); (2) INFRA.md's System map GitHub Actions line was stale in two ways — it still quoted ERCOT's original (pre-08-29-fix) ~13:30 UTC cron time, and it omitted JP entirely, though JP has ticked via GitHub Actions since 2026-08-29 (same day as the ERCOT cron fix commit). A third finding (JP's fetcher has produced zero receipts in 4 live days — `validate_prices`'s EUR-denominated sanity bounds [-500, 4000] reject virtually all JPY-denominated prices, since ¥/MWh nominal values run ~150-160x larger than EUR/MWh for an economically ordinary price; e.g. 11250 ¥/MWh ≈ 70 EUR/MWh, a routine off-peak price, exceeds the 4000 ceiling) is a core `loop.py` sanity-rail change outside the infra-resolver's narrow mechanical band (workflow files / doc drift / script wire-up / monitoring code only) — escalated to the user rather than fixed here, per the resolver's own scope. | machinery (infra-continuity auditor, second pass) | ERCOT's single cron replaced with a 3-entry retry ladder (03:23 / 07:23 / 11:23 UTC, each independently >=3h37 before the 15:00 UTC / 10:00 CT hard deadline; the tick is idempotent so redundant on-time attempts are free no-ops); INFRA.md's GitHub Actions line corrected to name both ERCOT and JP, and now points at ercot.yml for the exact schedule instead of hardcoding a minute prone to drift again. JP's fetcher currency-bound bug: escalated 2026-09-01 → AUTHORIZED + FIXED by the user 2026-09-02 — `validate_prices` made per-currency (`PRICE_BOUNDS`: EUR/GBP -500..4000, USD -500..9000 for ERCOT scarcity, JPY -1000..400000), so JEPX ¥/MWh and ERCOT scarcity prints now pass; the call site passes `market.currency`. Also closed the detector gap that let JP hide: `check_liveness` gained a never-populated-despite-ticking rule (>= NEVER_FETCH_TICKS OTS manifests but no price stored => STALE) — the GB stale-price fix (2026-08-29) only caught prices that WENT stale, not prices NEVER populated, which is why the monthly infra auditor (not the liveness monitor) was the one to catch JP. JP re-fetch triggered so it finally seeds prices.json and begins accruing a track record (from ~now; the ~5 launch days produced nothing). | test_ercot_schedule_has_a_retry_ladder + test_ercot_schedule_entries_stay_before_the_commit_deadline (tests/test_workflow_schedules.py — regex-parses the workflow's cron entries, fails if collapsed back to one shot or if any entry loses its deadline margin); test_infra_md_names_every_actions_driven_market (tests/test_docs_in_sync.py — fails if a future Actions-driven market isn't named on the System map line). JP fix (2026-09-02): test_validate_prices_bounds_are_currency_aware (JP/ERCOT prints pass under their currency, the exact JP value still fails under EUR, corrupt values still refused) + test_never_populated_prices_despite_ticking_is_broken (a market ticking for days with an empty prices.json is STALE, not exempt NEVER) |

| 2026-09-07 | Independent audit found GB's public page CONTRADICTING its own ledger (F1) and an undisclosed structural property of the GB feed (F2). F1: gb.html said "Live - awaiting first settled day" and labelled all 16 receipts "Pending" while Data/gb/ledger.jsonl held 12 settled shadow rows (climatology + weekly, 08-31..09-05, all re-derived exactly); VERIFY.md's GB bullet ("no settled day yet") was false as written. Cause: render_dashboard.build() switched to the awaiting template whenever the PRIMARY had no settlement, ignoring shadow settlements. F2: the primary (persistence) has ZERO GB receipts across every target day (rank-blend too) — the Elexon Market Index (APXMIDP) is a traded within-day index that populates progressively through the delivery day (git history of Data/gb/prices.json: each tick's last stored hour is ~1h before the tick), NOT the day-ahead auction the market docstring claimed publishes "~10:00 UTC"; persistence needs yesterday's complete profile at the tick, which this feed can never provide, so the primary structurally never commits. Leak guard unaffected (no price for T exists at commit); no arithmetic/append-only/OTS failure. Neither the page nor VERIFY.md disclosed any of it. | machinery (independent auditor) | F1: build() now renders the awaiting page only when the ledger is EMPTY; when shadows have settled but the primary has not, a shadow-only page shows the settled strategy panel, marks only unsettled receipts pending, and states that the primary has committed no receipt (0 of N target days). F2: the registry's Presentation gained a `note` (market disclosure, rendered verbatim as a Disclosure banner on every template) — GB's states the within-day-index feed and the never-committing primary; VERIFY.md's GB bullet rewritten with both disclosures; the GB docstring corrected; ARCHITECTURE.md gained the GB note. Whether GB should move to a genuine day-ahead source (or stay public under this framing) is ESCALATED to the user — not changed. | test_shadow_settlements_are_never_hidden_behind_the_awaiting_page (exact GB shape; fails on the pre-fix primary-only switch) + test_awaiting_page_only_when_nothing_at_all_has_settled + test_market_disclosure_note_renders_on_every_template + test_gb_discloses_its_within_day_index_feed |
| 2026-09-07 | Same audit, four stale-but-understated disclosures and one checker overclaim: (a) de.html and ercot.html footers quoted Spain's "~13:15 CET" as their publication time (SMARD DE-LU ~12:40-13:00 CET; ERCOT DAM ~13:30 CT) — the renderer's template had ONE hardcoded clock for every market; (b) VERIFY.md's DE and ERCOT bullets still said proofs "pending / not yet Bitcoin-anchored" though 18/20 (DE) and 11/11 (ERCOT) were confirmed; (c) ARCHITECTURE.md's Markets table still listed ERCOT as "Pages (git-attested)"; (d) VERIFY.md's ES evidence boundary was imprecise — the mirror's first commit (2026-08-10 ~22:59 UTC) post-dates that day's publication, so the target-2026-08-11 receipt is also not weak-check-corroborated (per-tick corroboration starts at target 08-12; 08-11 rests on the Bitcoin-confirmed strong check); (e) scripts/verify_ledger.py's DEFAULT output said "manifests anchored" / "receipts Bitcoin-covered N/N" although without --verify-ots it only checks that a .ots file exists — on audit day the 09-07 receipts were counted "Bitcoin-covered" while covered only by still-PENDING 09-06 proofs (the same overclaim class the pages were cured of 2026-08-29). All understate or mis-describe evidence; no arithmetic/money error. | machinery (independent auditor) | (a) Presentation gained a per-market `publication` label; ES/DE/ERCOT/GB registry entries carry their own; both templates' footers use it (generic fallback, never another market's clock). (b)+(c)+(d) prose corrected in VERIFY.md / ARCHITECTURE.md, phrased so they do not re-stale (point at `ots info` for per-date state). (e) checker's default wording changed to "OTS-stamped" / "OTS-covered … Bitcoin confirmation NOT checked here — pass --verify-ots"; VERIFY.md section 2 says the same. | test_footer_publication_time_is_per_market + test_every_public_market_states_its_own_publication_label (a public market with no label, or one quoting 13:15 outside ES, fails CI); test_default_report_never_claims_bitcoin_finality (the default checker report must not say anchored/Bitcoin-covered). (b)-(d) prose-only: the weekly auditor is the recurring detector for that class. |

Escape rate: **9 of 24 incidents were escapes** (recounted 2026-09-07 on the
resolver pass; both new rows are machinery catches, not escapes).
The prior count (9 of 19, as of 2026-08-30) is unchanged in numerator — see
its own note below for the 08-21→08-30 history. An escape = first noticed by
a human or an agent-session, not by machinery (e.g. the 2026-08-28 FR mirror
leak, the 2026-08-29 OTS overclaim, the 2026-08-30 month-end drift); machinery
wins — like the 2026-08-21 OTS-rewrite catch, the closed loop's first, and
both 2026-08-31 rows above — are NOT escapes. This number is the
justification for the closed-defect-loop apparatus — and the number the next
60 rolling days must drive to zero for the core to be declared reliable.

Review findings (leak-guard clock hole, torn-write risk, etc.) are tracked
in the project's internal backlog (not part of this public mirror), not
here — this file is for things that FIRED.
